import { Button } from '@mui/material';
import { h } from 'preact';

export const App = () => {
  return (
    <div id="preact_root">
      <p>Hello World</p>
      <Button variant="contained">Click me</Button>
    </div>
  );
};
